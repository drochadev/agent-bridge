"""OpenCode adapters: the public bridge to a real local OpenCode agent.

opencode run CLI (v1.18.31, inspected locally — the installed CLI is the
source of truth, not online docs). Nothing here imports private mediator
code; composition with core protocols only.
"""
